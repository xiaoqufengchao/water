version: 1.0.{build}

build: false

environment:
  matrix:
    - PYTHON: "C:\\Python27"
      PYTHON_VERSION: "2.7.8"
      PYTHON_ARCH: "32"

  TST_PUB_DWNL: './tst_pub_dwnl_David_etal_201x_SER.sh'
  TST_PUB_REPR: './tst_pub_repr_David_etal_201x_SER.sh'
  DING: './test.sh'

install:
  - ps: Remove-Item Alias:wget
  - ps: choco install -y (get-content requirements.win)

test_script:
    #- ps: mkdir input
    #- ps: wget -nv -nc www.goole.com -P input -o out
    #- ps: wget -nv -nc www.google.com -P input -o out
    #- ps: cat input/index.html
    #- ps: cat out
  - ps: cd ./tst
  - ps: sh cat.sh
  - ps: sh tst_pub_dwnl_David_etal_201x_SER.sh
